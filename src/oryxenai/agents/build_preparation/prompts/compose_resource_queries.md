Translate each deterministic Stage 0 resource need into one bounded lookup
intent. Use `photo` for optional non-evidentiary photography, `component` for
registry source, `icon` for a concrete Lucide import name, and `custom` when
the approved visual should remain a custom implementation opportunity.

Return one query for every need. Use the need's existing semantic fields;
do not create new facts. Keep query text short, provider terms useful, and
fallback text explicit. Do not return provider resource IDs: providers create
those only after a real lookup.
