Translate each deterministic Stage 0 resource need into one bounded lookup
intent. Use `photo` for approved non-evidentiary editorial photography,
`component` only for a concrete interaction primitive that needs registry
source, `icon` for a concrete Lucide import name, and `custom` when the
approved visual should remain a local implementation opportunity. Hero
patterns, background systems, and representative diagrams are custom unless
the need explicitly requires an image or component.

Return one query for every need. Use the need's existing semantic fields;
do not create new facts. Keep query text short, provider terms useful, and
fallback text explicit. Do not return provider resource IDs: providers create
those only after a real lookup.
