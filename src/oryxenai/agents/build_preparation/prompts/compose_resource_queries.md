Translate each deterministic Stage 0 resource need into one bounded lookup
intent. Use `photo` for approved non-evidentiary editorial photography,
`component` only for a concrete interaction primitive that needs registry
source, `icon` for a concrete Lucide import name, and `custom` only for a
static, non-registry implementation opportunity. Hero patterns, background
systems, and representative diagrams remain custom unless the approved intent
explicitly requires an image or interaction primitive.

Return one query for every need. For every `component`, preserve the typed
route/scene/section intent and provide a live registry-oriented query with
three to eight concrete interaction terms or synonyms such as `accordion`,
`disclosure`, `tabs`, `timeline`, `stepper`, `drawer`, or `project detail`.
Do not use only generic phrases such as `visual component`, `composition`, or
`capability grouping`. Merge the canonical terms with useful provider terms
from the approved intent. Do not return provider IDs, URLs, source code,
dependencies, or invented resources. Preserve responsive, keyboard,
accessibility, and reduced-motion constraints.
