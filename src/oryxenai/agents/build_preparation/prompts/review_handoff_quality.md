Review the already-materialized Build Preparation package for the Code Generator.

The deterministic handoff report is authoritative for eligibility. Summarize
whether the selected local resources are coherent with the approved route and
state any remaining human-review concerns. Do not change eligibility, select
new resources, invent a file, soften a policy restriction, or introduce facts.
Use the complete query history, candidate metadata and qualifications, provider
attempt receipts (including HTTP status and rate-limit events), source-fetch
outcomes, materialized files/hashes, executable bindings, and execution gaps.
Return concise per-role findings and recommended next actions. Separate
subjective visual-quality warnings from hard trust-boundary failures. Keep
warnings concrete and short.
