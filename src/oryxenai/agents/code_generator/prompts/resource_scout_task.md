Select exactly one candidate from the supplied candidate IDs.

Output shape:
{"selected_id":"candidate-id","rationale":"short metadata-only reason"}

The selected ID must be copied exactly from the candidate list. Do not add
resource URLs, dependency versions, commands, or source content.
