Integrate the completed portfolio through the explicitly owned integration paths.
The runtime shell under `src/app/**` and `src/main.tsx` is trusted and immutable;
inspect it and integrate against its stable interfaces. Reconcile the SitePlan,
route coverage, shared component contracts, local resource ledger, and verified
source manifest into one cohesive portfolio. The `<generation-contract>` block
is normative and lists every route anchor and literal survival requirement.

The trusted shell already wires `src/generated/route-registry.ts`, provides
landmarks, skip-link behavior, navigation, and the visible unknown-path
`<h1>Page not found</h1>` fallback. Never replace that behavior or route an
unknown pathname to home. Do not modify `src/generated/**`, `src/content/**`,
`src/app/**`, `src/main.tsx`, route files under `src/routes/**`, scaffold
configs, or package files. If a defect blocks integration, report it honestly.

Audit visual sameness and correct it through planned composition, typography,
spacing, and interaction systems rather than generic ornament. Confirm spacing
uses the design tokens, headings use the fluid scale, every declared
interaction is keyboard-accessible and reduced-motion-safe, and every resource
reference resolves to an admitted local binding. Route anchors must retain
route IDs, section IDs, approved copy, acceptance markers, and interaction
attributes exactly.

Use the local resource ledger as implementation input. A required image must
be rendered through its `/resources/pack/...` URL. A required component source
must be imported from `src/generated/resources/pack/...` and rendered; a slot
ID, filename in a comment, or manifest mention is not a binding. Use package
icons only through their admitted package/export. Do not invent facts or
placeholder copy, introduce network dependencies, or modify manifests.

Return the required integration report only when its statements are evidenced
by the source tree and input markers. Fill `self_check` honestly.
