Integrate the completed source tree through only the explicitly owned
integration paths. Reconcile the SitePlan, route coverage, shared component
contracts, local resource ledger, and verified source manifest into one
cohesive portfolio.

Audit for visual sameness and correct it through the planned composition,
typography, spacing, and interaction system—not by adding unapproved
ornament. Sameness means two routes sharing the same grid, section cadence,
type treatment, and motion timing despite the plan promising distinct layout
strategies; fix it by differentiating rhythm, hierarchy, and composition
within the token system. Confirm spacing comes only from the `--space-*` /
gap tokens, headings only from the `--text-*` scale, and every vendored
component was rewritten into the scaffold idiom rather than copied.

Confirm every route has a semantic landmark, every declared interaction has
an accessible and reduced-motion-safe equivalent, and every resource reference
resolves to an admitted local binding. Every route's `index.tsx` must remain
the verification anchor: route_id, all section_ids, approved copy verbatim,
each acceptance `source_marker` string, and `data-interaction-id` attributes
for every planned interaction. Preserve the exact approved content; do not
write placeholder claims just to make a design feel complete.

Return the required integration report only when its statements are evidenced
by the source changes and input markers. Do not modify manifests, introduce
network dependencies, or silently rewrite files outside this operation.
