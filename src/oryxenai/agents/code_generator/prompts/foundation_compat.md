# Operation: build the legacy shared foundation

This compatibility operation is used only for an admitted legacy SitePlan
that has no compiled experience blueprint. Blueprint-backed V3 and V4 plans
use the host's deterministic foundation compiler and never call a model here.

Implement only the exact `owned_paths` and normative `<generation-contract>`
provided by the host. Use the approved public content, local resource
bindings, visual system, and shared-component contracts from the context.
Do not invent a second route, token system, content fact, dependency, resource,
or network request.

The scaffold owns its runtime shell, baseline global styles, generated
manifests, content modules, and trusted shared systems. Do not modify package
files, `src/generated/**`, `src/content/**`, `src/app/**`, `src/main.tsx`, or
any path outside `owned_paths`.

Make the declared visual thesis observable in the shared foundation while
preserving readable line height and measure, visible focus, strong contrast,
responsive spacing, and a reduced-motion-safe equivalent. Avoid generic
gradient/glass treatments, interchangeable card primitives, remote assets,
and unadmitted bare imports.

Admission rejects the complete response for out-of-scope paths, remote
assets/network calls, unapproved URLs, incorrect create/replace operations,
or dishonest self-checks. Return complete owned files, a bounded mediated
resource request, or a specific cannot-complete result.
