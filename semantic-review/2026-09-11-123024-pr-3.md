# Fail-closed Build Preparation dispatch with stable legacy projections

Task 3 now distinguishes current raw, current namespaced, legacy-declared namespaced, and unversioned compatibility inputs before applying format-specific validation and projection behavior. The four findings from the prior pass are closed: old namespaced plural inputs retain their historical topology, repeated raw section IDs are canonicalized per route, old declared namespaced records retain their historically optional fields, and current owner expansion rejects synthesized ID collisions. The original duplicate-key, strict-type, nested dependency-authority, complete ownership, projection-drift, version-continuity, and dispatch-provenance goals remain enforced. No new compatibility or security regression was found at the reviewed intersections.

Watch for: **confirmed** no unresolved issue; the intentional compatibility boundary freezes historical namespaced topology while only current raw and explicitly current namespaced formats receive per-route owner expansion.

**Verdict**: APPROVED

## High-level view

`brief_ingestion.py::_prepare_brief_indexes` selects format before applying current-only producer requirements. A `build-preparation-brief-v1` declaration paired with namespaced sections remains legacy, while `build-preparation-namespaced-brief-v1` identifies the current namespaced format; `brief_assembly.py::_brief_source_version` emits the declaration matching the actual IDs.

`brief_ingestion.py::_canonicalize_raw_content_index` maps local IDs by route, and `_section_content` resolves repeated headings from active `## Route` context. Producer output containing `home/hero` and `work/hero` therefore becomes two distinct canonical sections.

`brief_ingestion.py::_resource_contracts` preserves historical one-slot namespaced projections, expands current formats per route, and validates final synthesized slot and need IDs. `generation_orchestrator.py::CodeGeneratorGenerationOrchestrator.execute` then requires both admitted-identity equality and exact persisted projection-hash equality before plan validation, workspace access, or model execution.

<details>
<summary>Issues (0)</summary>

No actionable concerns.

</details>

<details><summary>Details</summary>

## Legacy namespaced wire and projection contracts remain frozen

`brief_ingestion.py::_prepare_brief_indexes` invokes `_validate_current_producer_fields` only for declared raw input and the explicit current namespaced version. A legacy `build-preparation-brief-v1` declaration with namespaced sections keeps `need_id`, resource `status`, and component `need_id` optional while retaining strict historical required fields, types, forbidden extras, and nested candidate allowlists.

`brief_ingestion.py::_resource_contracts` enables `modern_owner_projection` only for raw format or the explicit current namespaced version. Unversioned and legacy-declared namespaced inputs retain the old first-route, unsuffixed topology and `_role_sections` full namespaced-ID matching. In-memory differential probes executed the pre-change and current compilers over both legacy dispatch forms and the checked-in legacy fixture; contract identities, projections, and projection hashes were equal.

The focused regressions are `test_declared_legacy_namespaced_shape_keeps_historical_optional_fields`, `test_legacy_namespaced_plural_owner_projection_stays_frozen`, and `test_namespaced_legacy_declaration_preserves_historical_role_projection` in `tests/unit/agents/code_generator/test_development_input.py`.

## Repeated raw IDs stay route-owned through navigation and body parsing

`brief_ingestion.py::_canonicalize_raw_content_index` stores `raw section -> route -> canonical section`, rejects duplicates within one route, and expands a deduplicated navigation token to every canonical owner. `_section_content` resolves each repeated local heading through its active route; wrong-route headings fail with `BRIEF_SECTION_HEADING_SCOPE_INVALID`, and duplicate content for one canonical section fails cardinality validation.

`test_raw_v1_canonicalizes_repeated_route_local_section_ids` covers direct compilation. `test_current_build_preparation_producer_declares_and_compiles_raw_v1` exercises `brief_assembly.py::build_content_brief` with repeated `hero` IDs on two routes and verifies both canonical sections retain their own content.

## Current owner expansion rejects synthesized ambiguity

For current raw and explicit current namespaced input, `brief_ingestion.py::_resource_contracts` emits one slot, need, decision, section binding, and criterion binding per owner route. `seen_projected_slots` and `seen_projected_needs` check every final suffixed ID, preventing authored `shared@home` or `need@home` values from colliding with synthesized IDs across resources and components.

`test_route_expansion_rejects_synthesized_identifier_collisions` covers slot and need collisions. `test_plural_resource_ownership_expands_to_deterministic_route_local_slots` and the explicit-current branch of `test_legacy_namespaced_plural_owner_projection_stays_frozen` cover complete route-specific expansion.

## Strict admission and resume checks retain the original seven gates

`brief_ingestion.py::_strict_json_loads` rejects duplicate object keys recursively. `_validate_wire_shape`, `_validate_resource_candidate_wire`, and `_validate_component_suggestion_wire` enforce non-coercing types and nested allowlists before dispatch, while `_resolution` refuses dependency authority from brief candidates. `dispatch_mode`, `source_format`, `source_version`, and `structural_signature` persist through `InputReceipt` and mirror summaries.

`generation_orchestrator.py::CodeGeneratorGenerationOrchestrator.execute` requires exact equality between `run.input_receipt["projection_hashes"]` and the freshly compiled `InputReceipt.projection_hashes`, alongside admitted-identity equality. Missing, malformed, or changed hashes produce `INPUT_PROJECTION_DRIFT`. `test_generation_resume_rejects_projection_hash_drift_before_workspace_or_model` confirms rejection before workspace creation or any model call.

## Offline verification

The focused Code Generator input and Build Preparation integrator unit suites passed, as did the targeted PostgreSQL-backed projection-drift integration test. Ruff reported no findings on the scoped production and test files. No live pipeline, provider request, model call, campaign reservation, or generated workspace was started.

</details>

<details>
<summary>File map</summary>

- `src/oryxenai/agents/build_preparation/brief_assembly.py` — emits raw or namespaced declarations from actual section style.
- `src/oryxenai/agents/code_generator/core/brief_ingestion.py` — strict dispatch, route-local canonicalization, versioned owner projection, final-ID checks, and projection receipts.
- `src/oryxenai/agents/code_generator/core/development_input.py` — exposes source-format provenance in mirror summaries.
- `src/oryxenai/agents/code_generator/core/development_schemas.py` — persists dispatch provenance and projection hashes in `InputReceipt`.
- `src/oryxenai/agents/code_generator/core/generation_orchestrator.py` — rejects identity or projection-hash drift before execution.
- `tests/unit/agents/code_generator/test_development_input.py` — strict-wire, compatibility, canonicalization, ownership, collision, producer, and provenance regressions.
- `tests/integration/test_code_generator_generation_worker.py` — pre-workspace projection-drift rejection.

Full scoped diff: `git diff -- src/oryxenai/agents/build_preparation/brief_assembly.py src/oryxenai/agents/code_generator/core/brief_ingestion.py src/oryxenai/agents/code_generator/core/development_input.py src/oryxenai/agents/code_generator/core/development_schemas.py src/oryxenai/agents/code_generator/core/generation_orchestrator.py tests/unit/agents/code_generator/test_development_input.py tests/integration/test_code_generator_generation_worker.py`

</details>
