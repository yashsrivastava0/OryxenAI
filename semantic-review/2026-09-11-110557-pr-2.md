# Epoch-scoped integration-review call receipts

The remaining retry collision is resolved by deriving `GenerationCallReceipt` identity from the epoch-aware namespace already used for `GenerationAttemptRecord` identity. Receipt and idempotency identities remain stable during replay in one epoch, while an explicit epoch advance creates separate call history with unchanged context and round. The regression returns a changed valid review in the new epoch and verifies that each attempt links to its own receipt.

Watch for: No actionable semantic issue remains in this fix; the prior P1 is closed.

**Verdict**: PASS

## High-level view

Integration-review attempts and call receipts now cross the same explicit-epoch boundary. Redelivery within an epoch remains one logical attempt, while explicit retry no longer collides with or disappears into preserved receipt history.

The regression holds context and round constant across three calls: two in epoch zero and one changed-output call in epoch one. It verifies same-epoch deduplication, cross-epoch identity separation, and attempt-to-receipt linkage.

<details>
<summary>Issues (0)</summary>

No actionable semantic issues.

</details>

<details><summary>Details</summary>

## Epoch boundary now covers both sides of receipt linkage

The call key hashes `_generation_attempt_namespace(...)`, `integration_review`, the round, and returned context hash; `receipt_id` derives from that key and `idempotency_key` stores the full digest (`generation_orchestrator.py:2633-2644`). Attempt reservation independently hashes the same epoch namespace with its attempt dimensions (`generation_orchestrator.py:555-589`), and completion attaches the epoch-specific receipt to that attempt. Context receipts remain content-deduplicated without collapsing provider calls from separate explicit attempts.

The regression's first two calls produce one call receipt and one attempt record. After only `attempt_epoch` advances, the mock returns a different schema-valid accepted review, and the assertions require two receipt IDs, idempotency keys, and attempt IDs plus an exact match between attempt `call_receipt_id` values and stored receipts (`test_semantic_decline_reliability.py:786-895`). The changed output would have exposed the old collision, while the initial replay protects stable same-epoch identity.

</details>

<details>
<summary>File map</summary>

- `src/oryxenai/agents/code_generator/core/generation_orchestrator.py` — namespaces integration-review call receipt and idempotency identity by explicit attempt epoch.
- `tests/unit/agents/code_generator/test_semantic_decline_reliability.py` — covers same-epoch replay and changed-output explicit retry across epochs.

Full focused diff: `git diff main -- src/oryxenai/agents/code_generator/core/generation_orchestrator.py tests/unit/agents/code_generator/test_semantic_decline_reliability.py`

Validation: targeted regression (`1 passed`), full semantic-decline reliability module (`11 passed`), Ruff on both reviewed files, and `git diff --check`.

</details>
