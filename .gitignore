# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
build/
dist/
node_modules/
*.egg-info/
*.egg
pip-wheel-metadata/
.python-version

# uv
# uv.lock is COMMITTED intentionally

# Environment / secrets
.env
.env.*
!.env.example

# VM-local rendered production configuration and deployment state
config/app.production.local.toml
.workspace/azure-deploy/

# Local state / runtime
*.log
*.sqlite
*.sqlite3
oryxenai.db

# Workspace (all generated developer state)
.workspace/

# JavaScript dependencies in checked-in scaffold fixtures
**/node_modules/

# Legacy root caches (kept in case a tool runs outside canonical scripts)
.pytest_cache/
.mypy_cache/
.ruff_cache/
__pycache__/
*.pyc

# Testing / coverage
.coverage
.coverage.*
htmlcov/

# Editors / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# Docker
# compose.yaml is COMMITTED intentionally

# Local database data (never commit)
pgdata/
postgres-data/

# Scratch & temporary test artifacts
scratch_benchmark.json
scratch/
.pytest-tmp*
.uv-cache*

# Agent outputs (disposable local exports and debug mirrors). Everything
# under output/ is a disposable local courtesy export -- durable state
# lives in PostgreSQL and configured artifact storage. No example run is
# currently pinned/committed; the previous 2026-09-06-handoff pinned packs
# were retired once that handoff was resolved and superseded.
output/*
!output/README.md

# Generated Preact/Vite product bundle (frontend/, docs/Frontend/05 §16) —
# built by CI/container packaging, never hand-edited or committed.
src/oryxenai/web/static/product/
