# Image-generation prompts

Use the built-in image generation path, one image per prompt. These are `ui-mockup` references, not production artwork. Keep labels sparse and legible; the Markdown specification is authoritative for exact copy.

## 01-shell-overview.png

Use case: ui-mockup. Asset type: desktop product workspace reference. Show a serious agentic creative studio at 1536×695: warm paper background, deep ink typography, restrained cobalt accent, compact topbar, five-step stage navigator, one wide centered stage canvas, small context strip, visible sticky action dock, and a closed inspector affordance. One main surface only; no permanent left or right rail; no raw JSON visible. Avoid gradients, glassmorphism, dashboards, charts, fake percentages, and stock imagery.

## 02-discovery-intake.png

Use case: ui-mockup. Asset type: desktop Discovery intake reference. Show one focused intake field, concise explanatory copy, small prompt examples, word count, and one clear Start Discovery action inside a calm single-screen composition. Preserve generous whitespace without creating a scrollbar. Warm editorial Swiss styling, cream paper, ink, cobalt action. No JSON, utility rail, or oversized title.

## 03-discovery-review.png

Use case: ui-mockup. Asset type: desktop brief-review reference. Show compact brief title, status, short narrative summary, grouped profile facts, a collapsed full-brief reader, revision entry point, and a sticky bottom action dock with Approve and Revise. The action must be visible without a long document scroll. No six-line title, no raw JSON.

## 04-content-route-review.png

Use case: ui-mockup. Asset type: desktop content architecture reference. Show a route map across the top, accessible route tabs, one selected route, section cards with real-looking concise copy, a narrative thesis, and a visible approval/revision action dock. Use purposeful asymmetry but keep the text column readable. Do not show every route expanded simultaneously.

## 05-design-storyboard.png

Use case: ui-mockup. Asset type: desktop visual direction reference. Show creative thesis, prose intent cards for color behavior, typography behavior, motion, and interaction, then a visible numbered scene storyboard with route selector. Use neutral intent language; do not show hex colors, color swatches, font names, CSS values, or external images.

## 06-preparation-ready.png

Use case: ui-mockup. Asset type: desktop Build Preparation handoff reference. Show readiness status, compact route/section/resource/component counts, metadata evidence tiles with source/license/dimensions, two collapsed brief readers, and a visible Continue to Generate action. Use neutral placeholder evidence blocks instead of broken images or keyword alt strings.

## 07-generation-available.png

Use case: ui-mockup. Asset type: desktop generation-entry reference. Show approved Build Preparation context, a clear Generate Portfolio action, a concise explanation of the handoff, and no dead-end Stage Locked screen. Keep the shell focused and quiet.

## 08-generation-working.png

Use case: ui-mockup. Asset type: desktop agent-progress reference. Show semantic milestones Planning, Acquiring resources, Building pages, Testing viewports, and Promoting preview; emphasize the current milestone, show last updated/attempt information and a Stop action. No raw coordinator labels, percentage dials, terminal output, or JSON.

## 09-generation-attention.png

Use case: ui-mockup. Asset type: desktop failure-recovery reference. Show a direct Generation needs attention message, a safe summary, preserved-work note, a valid Retry generation or Refresh action, a preserved candidate/verified preview area, and a collapsed technical reference. Make the error calm and actionable, not alarming or vague.

## 10-mobile-review.png

Use case: ui-mockup. Asset type: mobile review reference at 390×844. Show compact topbar, current-stage selector, one-column artifact, route tabs contained in their own strip, readable cards, and a full-width reserved action dock. No horizontal page overflow, clipped labels, permanent rails, or decorative margin text.

## 11-output-inspector.png

Use case: ui-mockup. Asset type: developer-only inspector reference. Show the normal single-canvas product surface with a slide-over Output Inspector opened explicitly. The drawer contains selected stage label, safe status metadata, formatted read-only JSON, and a copy action. Keep it visually subordinate and clearly separate from the creator artifact. No secrets, prompts, raw worker payloads, or model reasoning.

## 12-admin-control-center.png

Intended viewport: 1536×695. Use case: ui-mockup. Asset type: administrator-only desktop product reference. Show a serious OryxenAI workspace administration surface using warm cream paper, deep ink typography, restrained cobalt accents, hairline rules, a compact topbar, administrator identity, Workspace/Refresh/Sign out controls, a live status line, safe account details, a six-tab list (`Users`, `Projects`, `Legacy`, `Deleted`, `Operations`, `Audit`), four summary metrics (`Active users`, `Projects`, `Running jobs`, `Pending operations`), and a clean Users ledger with masked metadata and compact lifecycle buttons (`suspend`, `reset entitlement`, `promote`, `delete`) plus `Load more`. Keep destructive buttons subordinate and make the screen read as a bounded ledger, not a creator stage. No creator stage navigator, raw JSON, external images, secrets, UUIDs, charts, fake percentage dials, stock photography, or permanent developer rail. This is a reference only; exact counts, names, timestamps, authorization, and action results come from the existing admin API and Markdown contract.

## 16-discovery-mcq-question.png

Use case: ui-mockup. Asset type: horizontal Discovery question reference for an implementation handoff. Intended viewport: `1536×695` application viewport only. Input images: Image 1 and Image 2 are current-state screenshots used only to understand the existing OryxenAI shell; do not reproduce their browser chrome, Windows taskbar, or open administrator popover. Primary request: show a premium, implementation-friendly Discovery multi-select question screen in the OryxenAI warm editorial Swiss product language. Scene/backdrop: calm warm cream paper canvas with restrained hairline grid/rules, no permanent side rails. Subject: one focused agent question and answer surface. Style/medium: polished high-fidelity web UI mockup, editorial serif display paired with a precise sans-serif utility layer. Composition/framing: compact unobscured topbar and five-stage navigator at the top; one centered readable question canvas; a small Discovery context strip; `Question 02 of 04`; concise question followed by one short why-this-matters hint; three vertically stacked full-row option tiles with native-looking checkbox indicators and one or two selected states; a reserved bottom action area with `Next question` as the cobalt primary and `Skip question` as quiet secondary; earlier answers collapsed. Lighting/mood: quiet, confident, thoughtful, breathable, precise. Color palette: warm cream, paper white, deep ink, graphite, restrained cobalt; no purple/blue AI glow. Materials/textures: matte paper surfaces, subtle rules, modest radius, no glass. Text (verbatim): `OryxenAI`, `IDEAS TO IMPACT`, `Discover`, `Content`, `Design`, `Prepare`, `Generate & Preview`, `DISCOVERY`, `Question 02 of 04`, `Which project stories should lead your portfolio?`, `A focused selection helps the portfolio make your strongest contribution clear.`, `Select all that apply`, `AlphaMesh-Core`, `Chronos-Tick-Fabric`, `NanoSecure-Risk`, `Next question`, `Skip question`, `Earlier answers`. Constraints: one screen only; option tiles must be clearly selectable; preserve generous spacing without empty dead space; show no invented backend metadata. Avoid: browser chrome, taskbar, open account menu, administrator controls, raw JSON, developer rail, model names, provider names, external URLs, stock imagery, broken media, gradients, neon, robot imagery, fake metrics, pill-button overload, inline run-on checkboxes, tiny unreadable text, and six-line headings.

## 17-discovery-text-question.png

Use case: ui-mockup. Asset type: horizontal Discovery free-text question reference for an implementation handoff. Intended viewport: `1536×695` application viewport only. Input images: Image 1 and Image 2 are current-state screenshots used only to understand the existing OryxenAI shell; do not reproduce their browser chrome, Windows taskbar, or open administrator popover. Primary request: show a premium, implementation-friendly Discovery text-question screen in the same OryxenAI warm editorial Swiss product language as the MCQ reference. Scene/backdrop: calm warm cream paper canvas with restrained hairline grid/rules, no permanent side rails. Subject: one focused agent question with a labeled answer composer. Style/medium: polished high-fidelity web UI mockup, editorial serif display paired with a precise sans-serif utility layer. Composition/framing: the same compact unobscured topbar and five-stage navigator; Discovery context strip; `Question 03 of 04`; a shorter conversational question; a visible `Your answer` label above a spacious multiline textarea; a useful example placeholder; a subtle `Draft saved` status; a reserved bottom action area with `Next question` as the cobalt primary and `Skip question` as quiet secondary. Lighting/mood: calm, human, considered, breathable, precise. Color palette: warm cream, paper white, deep ink, graphite, restrained cobalt; no purple/blue AI glow. Materials/textures: matte paper surfaces, subtle rules, modest radius, no glass. Text (verbatim): `OryxenAI`, `IDEAS TO IMPACT`, `Discover`, `Content`, `Design`, `Prepare`, `Generate & Preview`, `DISCOVERY`, `Question 03 of 04`, `What should someone understand after reading your portfolio?`, `A sentence or two is enough. Focus on the change you helped create.`, `Your answer`, `Describe the outcome, your contribution, or the decision behind the work…`, `Draft saved`, `Next question`, `Skip question`, `Earlier answers`. Constraints: one screen only; label must be visible above the field; do not show a numeric character limit because the current Discovery contract does not provide one; keep the field and actions above the fold. Avoid: browser chrome, taskbar, open account menu, administrator controls, raw JSON, developer rail, model names, provider names, external URLs, stock imagery, broken media, gradients, neon, robot imagery, fake metrics, pill-button overload, placeholder-only labeling, tiny unreadable text, and oversized multi-line headings.

## 13-code-generator-preview-workspace.png

Intended viewport: horizontal 16:10 desktop. Use case: Code Generator default workspace. Show a narrow left activity column with five semantic milestones, one current step, a multiline “Describe the next change…” composer, and a clear send action. Show a dominant right live-preview theater with browser frame, Desktop/Tablet/Mobile controls, fit/zoom controls, an interactive generated portfolio landing page, and a footer with Open preview plus Publish when ready. Use warm cream, ink, and restrained cobalt editorial styling. Keep the preview bounded inside its frame; no raw JSON, code editor, external URLs, neon gradients, fake metrics, or unbounded generated page.

## 14-code-generator-preview-working.png

Intended viewport: horizontal 16:10 desktop. Use case: Code Generator working state. Show Plan and Acquire complete, Build active with a small determinate progress cue, Verify and Preview pending, a follow-up composer, and a Stop generation action. The right preview theater is already present and fixed at 16:10, showing a partially built portfolio with neutral skeleton/evidence blocks and a “Preview updating as pages are verified” status. Publishing is visibly unavailable until verification. No raw logs, JSON, percentages as the primary narrative, or page overflow.

## 15-code-generator-preview-attention.png

Intended viewport: horizontal 16:10 desktop. Use case: Code Generator terminal failure and recovery. Show a calm red-bounded attention panel, preserved-work facts, stopped polling, retry eligibility, and Retry generation/Open details actions. Below it, show a large bounded browser preview marked Previous verified preview and an input for “Tell the agent what to fix…”. The preview remains interactive and contained. Keep publishing unavailable until verification recovers. No secrets, stack traces, raw JSON, external media URLs, or dead-end locked screen.

## Exact normalized generation prompts

The following are the normalized prompts used to generate the three files above. Reuse them only to understand the reference intent; do not regenerate or replace the images without an explicit request.

### 13-code-generator-preview-workspace.png

```text
Create a single horizontal 16:10 high-fidelity product UI reference image for the OryxenAI Code Generator workspace. This is an implementation reference screenshot, not a marketing illustration. Show a premium light editorial interface using warm cream paper, near-black ink, thin warm-gray rules, restrained cobalt-blue actions, elegant editorial serif display type paired with a clean grotesk sans.

Show a compact top bar with OryxenAI wordmark on the left and small session/status controls on the right. Below it, show a two-pane builder workbench: a narrow left pane around 32% width containing an ordered event timeline with five semantic milestones (Plan, Acquire, Build, Verify, Preview), tiny status markers, one current blue-highlighted event, and a composer at the bottom with a real multiline input box placeholder “Describe the next change…” plus a clear Send button. The right pane around 68% width is the primary live preview theater: a distinct browser-like frame with a small preview toolbar, device controls Desktop / Tablet / Mobile, a 16:10 constrained viewport containing a polished generated portfolio landing page with cream background, large short serif headline, calm image blocks, navigation, and a visible primary call-to-action. The preview must stay inside its frame with generous inner padding and no overflow; show subtle zoom/fit controls and a small “Preview • Live” indicator. Add a slim bottom action area under the preview with “Open preview” and “Publish when ready” hierarchy.

Make the layout feel like a real advanced AI builder: generous whitespace, clear hierarchy, low visual density, no dashboard-card spam, no neon gradients, no purple, no floating blobs, no random decorative illustrations, no fake analytics. Keep every control readable, alignment precise, and implementation-friendly. Do not show raw JSON, code editor, external URLs, literal hex values, or a giant oversized hero. Use abstract editorial portfolio copy only.
```

### 14-code-generator-preview-working.png

```text
Create one separate horizontal 16:10 high-fidelity product UI reference image for the OryxenAI Code Generator working state. This is a real implementation reference screenshot, not abstract art. Use the same visual system as a premium editorial creative workspace: warm cream paper background, near-black ink, thin gray rules, restrained cobalt-blue accent, editorial serif display type with clean grotesk UI text, spacious and calm.

Show a compact top bar with OryxenAI, a green “Session active” dot, and a small “Generating” status. Main workspace is a balanced split layout: left pane about 34% width with an event/activity timeline for the five generator milestones Plan, Acquire, Build, Verify, Preview. Plan and Acquire show small green completion markers; Build is the active cobalt step with a precise “Building pages” label and a thin progress indicator; Verify and Preview are pending. Under the timeline, show a visible multiline prompt composer with placeholder “Describe a change or ask about this build…” and a disabled-looking but readable Send button, plus a small note “You can keep working while we build.” Include a clear secondary “Stop generation” outline control.

Right pane about 66% width is a bounded live-preview theater. Use a browser frame with toolbar and device selector Desktop / Tablet / Mobile, the Desktop control active. Inside the fixed 16:10 preview viewport show a partially generated portfolio landing page with elegant cream background, top navigation, a large serif headline, two calm image placeholders with neutral evidence tiles, and a skeleton editorial block where the next section is being built. The preview should visibly remain clipped inside its viewport with a scrollbar or fit control, never expand the outer page. Add a small status strip under the frame: “Preview updating as pages are verified” and “Last update moments ago.” Add a bottom action bar under the preview with “Open preview” and a muted “Publish unavailable until verification completes.”

No raw JSON, code editor, floating blobs, purple/neon gradients, fake analytics, external URLs, or unexplained panels. Keep text short and readable, controls aligned, and the preview interaction affordances obvious.
```

### 15-code-generator-preview-attention.png

```text
Create one separate horizontal 16:10 high-fidelity product UI reference image for the OryxenAI Code Generator attention / recovery with preserved preview state. This is an implementation reference screenshot for a serious agentic creative workspace. Use the established visual system: warm cream paper background, near-black ink, thin neutral rules, cobalt-blue primary actions, a restrained muted red only for the error boundary, elegant editorial serif headings and clean grotesk interface text. No neon gradients, no purple, no generic dashboard clutter.

Show a compact top bar with OryxenAI, a small “Generation needs attention” status, and account controls. Main body is a two-column layout: left activity pane around 33% width with the five milestones Plan, Acquire, Build, Verify, Preview. Plan, Acquire, and Build are complete; Verify has a muted red attention marker and label “Verification stopped”; Preview is pending. Under the timeline, include a real follow-up instruction input with placeholder “Tell the agent what to fix…” and a blue Send button, plus a restrained “View technical details” link.

Right pane is the primary surface. At the top, show a clearly bounded attention panel with a red vertical rule or fine red border, warning icon, heading “Generation needs attention”, concise explanation “The build stopped during verification. Your saved work is safe.” Show three short facts: “Preview preserved”, “Polling stopped”, “Retry available”. Include prominent cobalt “Retry generation” button and secondary outlined “Open details” button. Below it, show a large preserved live-preview theater in a fixed 16:10 browser frame. Inside it, render an elegant portfolio landing page with a visible headline, image crop, top navigation, and a tiny badge “Previous verified preview”; the preview must remain within the frame, with no page explosion. Add a bottom toolbar under the preview with “Open preview” and a disabled or secondary “Publish after verification” control. Make the recovery path obvious without implying publishing succeeded.

The screen must look premium, calm, and implementation-ready: clear separation between agent activity and user-review surface, no raw JSON in the default surface, no code editor, no external URL, no fake analytics, no excessive cards, readable text, precise alignment, and generous whitespace.
```

### 20-generation-ready-preview.png

```text
Intended comparison viewport: 1536×695 application viewport; generated asset framing: horizontal 16:10 to match the existing Code Generator references. Use case: OryxenAI Code Generator verified ready/preview reference. This is a new desktop-only reference; do not create a tablet image. Show a compact unobscured OryxenAI topbar and five-stage navigator, a short “Generate & Preview” context strip, and one centered control-room canvas. Make the right side the dominant surface: a bounded browser-like preview theater containing an elegant generated portfolio page with warm cream background, deep ink typography, a restrained cobalt accent, a small route control, “Fit to view”, and a clear “Verified preview” status. The preview must stay inside a responsive 16:10-style frame and must not expand the host page. Keep a concise left context column with a short project heading, approved handoff summary, and completed semantic milestones “Plan”, “Acquire”, “Build”, “Verify”, “Preview”; it is secondary to the artifact. Add a truthful bottom action area with “Open verified preview” as the primary action and an optional quiet “Regenerate” action. Use no “Publish”, “Deploy”, or public URL claim because the current product contract has no publishing API. Keep diagnostics closed and omit raw JSON, admin menus, reset controls, browser/taskbar chrome, model/provider details, external asset URLs, stock imagery, gradients, neon glow, fake metrics, and oversized dashboard cards. Use the existing OryxenAI cream, paper white, ink, graphite, cobalt, hairline-rule, editorial-serif and precise sans-serif direction. This is a hierarchy reference; exact runtime copy, URLs, routes, verification state, and available actions come from the existing view model and server contract.
```
